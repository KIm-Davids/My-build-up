def minimum_number(number_inputted):
	minimum = number_inputted

	for number in number_inputted:
		if number > minimum:
			minimum = number
		return number


def maximum_number(number_inputted):
	maximum = number_inputted

	for numbers in number_inputted:
		if numbers > maximum:
			maximum = number
		return number

